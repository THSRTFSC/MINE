<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.mine"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="com.mine.framework.MINEApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.mine.module.splash.SplashActivity"
            android:theme="@style/AppTheme.NoTitle">

        </activity>

        <activity
            android:name="com.mine.module.mine.MINEActivity"
            android:theme="@style/AppTheme.NoTitle">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".module.login.LoginActivity"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".module.user.UserProfileActivity"
            android:theme="@style/AppTheme">

        </activity>
    </application>

</manifest>